name: ai_semantic_mobile_app
description: A Flutter mobile application integrating advanced AI capabilities and utilizing vector databases for efficient semantic searches.
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^5.0.0
  vector_math: ^2.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.0
  json_serializable: ^4.1.0
flutter:
  uses-material-design: true